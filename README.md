Sources for the plugin `shared.firebase.core`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "shared.firebase.core" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
